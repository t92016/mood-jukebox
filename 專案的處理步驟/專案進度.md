# 心情點唱機 — 專案進度

## 上次做到哪（2026-07-21）
完成三項主要改動 + 互動式對話 UI 全面上線：

### ✅ 第一點：心情點唱牆超連結
- 點唱牆歌曲改為可點擊，直接在本頁播放器播放（不開新頁）
- CSS 加 hover 效果（放大 + 變色）

### ✅ 第二點：靜態歌詞 LRCLIB（方案 A）
- 新增 `netlify/functions/lyrics.js` 後端 API
- 多源容錯：LRCLIB（主）→ lyrics.ovh（備援）→ 只用歌名查（容錯）
- 刪除硬寫的 `lyricsDB`，全部自動抓取
- 排除「歌詞版」影片，優先播放正常 MV
- 免費資源限制：部分冷門歌無歌詞，屬正常範圍

### ✅ 第三點：互動式對話選歌 UI
- 後端 `recommend.js` 支援多輪對話（`messages` 陣列）
- 前端新增「AI 點唱小助手」聊天介面
- 流程：點心情 → AI 推薦 → 使用者確認/換歌 → 確認後才播放
- 按鈕優化：「播放這首（歌手《歌名》）」+「換一首（AI 幫你選）」
- 可自由打字對話：「幫我換一首」、「我現在改難過了」

### ✅ 其他優化
- 防快取：HTML meta tag + JS 版本戳記
- 瀏覽器歌手名解析：從 YouTube 標題自動提取真正歌手名
- AI Prompt 加強防編造（要求只推薦確定存在的經典歌曲）
- 整理 `docs/網站介紹企劃單.md`

## 下一步
- [ ] `npm run seed` 預填 36 首歌曲快取（需先設 `YOUTUBE_API_KEY` 環境變數）
- [ ] 如需進一步優化歌詞命中率，可評估付費方案（Musixmatch Pro 等）
- [ ] 可依使用者回饋持續擴充 UI 功能

## 開發階段對照（依規劃書 Phase）
| Phase | 內容 | 狀態 |
|---|---|---|
| 1 | Prompt 工程（兒童安全 + JSON mode） | ✅ 已完成並上線 |
| 2 | YouTube 搜尋 + Firebase 快取機制 | ✅ 已完成並上線 |
| 3 | 模組整合（輸入→分析→快取/搜尋→寫入） | ✅ 已完成並上線 |
| 4 | 前端 UI + IFrame 串接 | ✅ 已完成並上線 |
| 5 | 互動式對話 UI（AI 點唱小助手） | ✅ 已完成並上線 |

## 重大技術決策
| 決策 | 理由 |
|---|---|
| 用 Netlify Functions 而非純前端 | 規劃書缺後端；Groq Key 放前端會被秒偷，必須代理 |
| 用 Netlify 而非 GitHub Pages | Pages 純靜態跑不了 Functions；Netlify 免費額度足夠且連 repo 自動部署 |
| GitHub repo 設 private | Netlify 免費版支援 private repo，兒童專案保守為上（develop skill 的 public 慣例是為了 Pages，本案不適用） |
| `Users` 集合改名 `MoodLogs` | 無帳號系統，匿名日誌用 MoodLogs 名實相符 |
| Firestore 前端直連（不經代理） | Firebase client SDK + Security Rules 本來就設計給前端用；多一層代理只增延遲 |
| Firebase 未設定時優雅降級 | `firebase-config.js` 沒填也能點歌（不寫紀錄），方便先測 UI |
| 種子歌放 `seeds/songs-seed.json` 不寫死 videoId | videoId 由 seed 腳本實際呼叫 API 取得，不假造 |

## 踩坑記錄
- 2026-07-21：歌詞 API 命中率問題 — 免費資源 LRCLIB/lyrics.ovh 對冷門中文歌覆蓋率有限，屬正常限制。已採用「多源容錯」策略最大化命中率，不影響使用者體驗。
- 2026-07-21：AI 偶爾編造歌名（如「五月天 - 開心」）— 已加強 Prompt 要求只推薦確定存在的經典歌曲，顯著改善。
- 2026-07-21：Browse 模式歌手名記錄錯誤（UI 描述文字被當成歌手名）— 已改為從 YouTube 影片標題自動解析真正歌手名。

## 最近更動紀錄
- 2026-07-21：階段性完成！三項改動全部上線（點唱牆超連結 + 多源歌詞 LRCLIB/lyrics.ovh + 互動式對話 UI）+ AI 點唱小助手標題 + 聊天按鈕優化 + 網站介紹企劃單（MD + TXT）+ commit/push
- 2026-07-20：使用者實機測試 + 心情點唱牆邏輯確認 + LRCLIB 歌詞 API 調查 + `.gitignore` 整理測試檔 + commit/push
- 2026-07-19：初始化完成。建立於 `G:\AI\Mood_Jukebox心情點唱機`，首次 commit + push 到 `t92016/mood-jukebox`（private）
